{
  "entities": {
    "Client": {
      "title": "Client",
      "description": "A client record containing personal details and notes.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The full name of the client."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The email address of the client."
        },
        "phone": {
          "type": "string",
          "description": "The phone number of the client."
        },
        "notes": {
          "type": "string",
          "description": "General notes about the client."
        },
        "authorUid": {
          "type": "string",
          "description": "The UID of the user who created this client record."
        },
        "createdAt": {
          "type": "timestamp",
          "description": "The time the client record was created."
        }
      },
      "required": ["name", "authorUid", "createdAt"]
    },
    "Consultation": {
      "title": "Consultation",
      "description": "A record of a consultation session with a client.",
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The ID of the associated client."
        },
        "date": {
          "type": "timestamp",
          "description": "The date and time of the consultation."
        },
        "notes": {
          "type": "string",
          "description": "Notes taken during or after the consultation."
        },
        "status": {
          "type": "string",
          "enum": ["scheduled", "completed", "cancelled"],
          "description": "The current status of the consultation."
        },
        "authorUid": {
          "type": "string",
          "description": "The UID of the user who created this consultation record."
        },
        "createdAt": {
          "type": "timestamp",
          "description": "The time the consultation record was created."
        }
      },
      "required": ["clientId", "date", "status", "authorUid", "createdAt"]
    }
  },
  "firestore": {
    "/clients/{clientId}": {
      "schema": {
        "$ref": "#/entities/Client"
      },
      "description": "Stores client records."
    },
    "/consultations/{consultationId}": {
      "schema": {
        "$ref": "#/entities/Consultation"
      },
      "description": "Stores consultation records."
    }
  }
}
